import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const AddressTitle = styled(Box)(() => ({
  width: "100%",
  height: "5rem",
  padding: "1rem 1.5rem 1rem 1.5rem",
}));
